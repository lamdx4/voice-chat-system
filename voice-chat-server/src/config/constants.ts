// Host grace period in milliseconds (30 seconds)
export const HOST_GRACE_PERIOD = 30 * 1000;

// Cleanup check interval (every 10 seconds)
export const CLEANUP_CHECK_INTERVAL = 10 * 1000;

// Enable hostless mode when host disconnects
export const ENABLE_HOSTLESS_MODE = true;

// Maximum participants in a group call
export const MAX_GROUP_PARTICIPANTS = 50;

// Maximum direct call participants (always 2)
export const MAX_DIRECT_PARTICIPANTS = 2;

